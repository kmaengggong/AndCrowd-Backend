<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.fiveis.andcrowd.repository.DynamicCrowdQna">

    <update id="createDynamicCrowdQnaTable">
        CREATE TABLE crowd_qna_#{crowdId}(
            crowd_qna_id INT AUTO_INCREMENT PRIMARY KEY ,
            crowd_id INT NOT NULL ,
            qna_title VARCHAR(200) NOT NULL ,
            qna_content VARCHAR(2000) NOT NULL ,
            publishe_at DATETIME DEFAULT now() ,
            updated_at DATETIME DEFAULT now() ,
            is_deleted BOOL default false
        );
    </update>

    <select id="findAll">
        SELECT
            crowd_qna_id AS crowdQnaId ,
            crowd_id AS crowdId ,
            qna_title AS qnaTitle ,
            qna_content AS qnaContent ,
            publishe_at AS publishedAt ,
            updated_at AS updatedAt ,
            is_deleted AS isDeleted
        FROM
            crowd_qna_#{crowd_id}
        ORDER BY
            crowdQnaId DESC
    </select>

    <select id="findById" parameterType="int">
        SELECT
            crowd_qna_id AS crowdqnaId ,
            crowd_id AS crowdId ,
            qna_title AS qnaTitle ,
            qna_content AS qnaContent ,
            publishe_at AS publishedAt ,
            updated_at AS updatedAt ,
            is_deleted AS isDeleted
        FROM
            crowd_qna_#{crowdId}
        WHERE
            crowd_qna_id = #{crowdQnaId}
    </select>

    <insert id="save" parameterType="com.fiveis.andcrowd.dto.DynamicCrowdQnaDTO$Save">
        INSERT INTO
            crowd_qna_#{crowdId} (crowd_id, qna_title, qna_content)
        VALUES
            (#{crowdId}, #{qnaTitle}, #{qnaContent})
    </insert>

    <update id="update" parameterType="com.fiveis.andcrowd.dto.DynamicCrowdQnaDTO$Update">
        UPDATE
            crowd_qna_#{crowdId}
        SET
            qna_title = #{qnaTitle},
            qna_content = #{qnaContent},
            updated_at = now()
        WHERE
            crowd_qna_id = #{crowdQnaId}
    </update>

    <delete id="deleteByCrowdQnaId" parameterType="int">
        DELETE FROM
            crowd_qna_#{crowdId}
        WHERE
            crowd_qna_id = #{crowdQnaId}
    </delete>

</mapper>